fastapi>=0.95
uvicorn[standard]>=0.22
streamlit>=1.28
requests>=2.31
pandas>=2.1
plotly>=5.18
psutil>=5.9
python-dotenv>=1.0
openai>=1.3
# LangChain pinned to a version compatible with langchain-community
# LangChain pinned to specific compatible versions
langchain==0.1.13
langchain-community==0.0.29
langchain-openai==0.0.5
# faiss-cpu is optional and often fails to build inside minimal Docker images.
# If you need vector DB support on the host/EC2, install faiss-cpu manually
# following your distribution's recommendations or use a prebuilt wheel.
# To enable it in the image, uncomment the next line.
# faiss-cpu
